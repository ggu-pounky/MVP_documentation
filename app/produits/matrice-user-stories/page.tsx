export default function MatriceUserStoriesPage() {
  return (
    <div className="min-h-screen bg-[rgb(var(--background-start-rgb))] text-[rgb(var(--foreground-rgb))] p-8">
      <h1 className="text-3xl font-bold mb-6 text-[rgb(var(--primary-rgb))]">Matrice de traçabilité USER STORIES</h1>
      <div className="bg-[rgb(var(--card-rgb))] border border-[rgb(var(--card-border-rgb))] rounded-xl p-6 shadow-lg min-h-[80vh]">
        <p className="text-[rgba(var(--secondary-rgb),0.7)]">
          Matrice de traçabilité pour les USER STORIES.
        </p>
        {/* Contenu à développer */}
      </div>
    </div>
  );
}
